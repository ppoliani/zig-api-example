{
  "version": "0.2.0",
  "configurations": [
    {
        "name": "debug",
        "type": "lldb",
        "request": "launch",
        "program": "${workspaceFolder}/zig-out/bin/zig-api-example",
        "args": [],
        "env": {
          "ENV": "development"
        },
        "cwd": "${workspaceFolder}",
        "preLaunchTask": "zig-debug-build"
    }
  ]
}
